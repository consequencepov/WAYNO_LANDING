const fs = require('fs');
const file = 'frontend/src/components/sections/SectionTransformation.tsx';
let content = fs.readFileSync(file, 'utf8');

const updatedSection = `export function SectionTransformation() {
  return (
    <section className="relative bg-[#0a0a0a] z-20 py-32 px-6">
      <div className="sticky top-[10vh] text-center mb-16 max-w-2xl mx-auto z-0 opacity-50 transition-opacity duration-500">
        <h2 className="text-4xl md:text-5xl font-medium tracking-tight text-white mb-6">
          От референса <span className="text-white/40">к продукту.</span>
        </h2>
        <p className="text-lg text-white/60">
          Передайте ссылку или скриншот. WAYNO не ворует, а понимает логику и собирает уникальный проект.
        </p>
      </div>

      <div className="w-full relative max-w-5xl mx-auto flex flex-col gap-[50vh] pb-[20vh] z-10">
        {STEPS.map((step,const fs = require('fs');
const fileepconst file = 'frontend/sinlet content = fs.readFileSync(file, 'utf8');

const updatedSection = `expnt
const updatedSection = `export function Setio  return (
    <section className="relative bg-[#0a0a0a] z-20 py `    <sectCa      <div className="sticky top-[10vh] text-center mb-16 max-{
        <h2 className="text-4xl md:text-5xl font-medium tracking-tight text-white mb-6">
          От референса {           От референса <span className="text-white/40">к продукту.<sN        </h2>
        <p className="text-lg text-white/60">
          Передайте:p-12         <p c0px_50px_rgba(0,0,0,0.4)] flex flex-col md:flex-        </p>
      </div>

      <div className="w-full relative max-w-5xl mx-auto flex flex-col gap-[50vh] pb-[20vh] z-10">
        {STEPS.map((step,const fs = require('fs');
cod:      </div-2
      <divx-c        {STEPS.map((step,const fs = require('fs');
const fileepconst file = 'frontend/sinlet conole.log('done');
