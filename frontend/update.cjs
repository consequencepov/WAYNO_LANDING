const fs = require('fs');

const content = `import { ArrowUp, Plus, Monitor, Palette, X, FileText } from 'lucide-react'
import { useState, useRef } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { cn } from '@/lib/cn'
import { useStore } from '@/store/useStore'

export function PromptInput({ className }: { className?: string }) {
  const { promptText, setPromptText, brandMode } = useStore()
  const [isFocused, setIsFocused] = useState(false)
  const fileInputRef = useRef<HTMLInputElement>(null)
  
  const [file, setFile] = useState<{name: string, url: string} | null>(null)
  const [copySiteMode, setCopySiteMode] = useState(false)
  const [showDesignPicker, setShowDesignPicker] = useState(false)
  
  const placeholders = {
    professional: "Опишите проект, задачу или вставьте URL-референс...",
    entrepreneur: "Опишите идею продукта, оффер или гипотезу..."
  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { useState, useRef } from 'react'
import { motion, AnimatePresence } from 'framer-moceimport { motion, AnimatePresence } from  import { cn } from '@/lib/cn'
import { useStore } fromnpimport { useStore } from '@/Si
export function PromptInput({ className }nde  const { promptText, setPromptText, brandMode } = useStore()
  congn  const [isFocused, setIsFocused] = useState(false)
  const ML  const fileInputRef = useRef<HTMLInputElement>(arge  
  const [file, setFile] = useState<{name: string, = UR  const [copySite(selectedFile)
      setFile({ name: selectedFile.name, url   const [showDesignPicker, setShowDesignPicker] = useStaic  
  const placeholders = {
    professional: "Опишите пeE en    professional: "Опfa    entrepreneur: "Опишите идею продукта, оффер или гипотезу..."
  }
  
   {  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { useState, n(       
con ?
const content = `import?аimpo trconstiimport { motion, AnimatePresence } from 'framer-moceimp       onMouimport { useStore } fromnpimport { useStore } from '@/Si
export function PromptInput({ className }nde  const { promptTextalexport function PromptInput({ className }nd items-center   congn  const [isFocused, setIsFocused] = useState(false)
  const ML  const fileInputRef = useRef<HTMLIor  const ML  const fileInputRef = useRef<HTMLInputElement>5r  const [file, setFile] = useState<{name: string, = UR  const [ck      setFile({ name: selectedFile.name, url   const [showDesignPicker, setShowDesigam  const placeholders = {
    professional: "Опишите пeE en    professional: "Опfa    entrepren</    professional: "Оп

  }
  
   {  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { useState, n(       
con ?
const content = `import?аimpo trconsti           
  cssNam
con ?
const content = `import?аimpo2reconsorcon ?
const content = `import?аimpo trconstiimport {r-conshaexport function PromptInput({ className }nde  const { promptTextalexport function PromptInput({ className }nd items-center   congn  const [isFocused, setIsFocuse/[  const ML  const fileInputRef = useRef<HTMLIor  const ML  const fileInputRef = useRef<HTMLInputElement>5r  const [file, setFile] = useState<{name: string, = UR  const [ck      set1r    professional: "Опишите пeE en    professional: "Опfa    entrepren</    professional: "Оп

  }
  
   {  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { useState, n(       
con ?
const content = `import?аimpo trconsti            
  }
  
   {  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { usesca  :  .9  
  c     
con ?
const content = `import?аimpo5, cons: con ?
const content = `import?аimpo trconsti          cons    cssNam
con ?
const content = `import?аimpo2rec[4con ?
crdconsorconst content = `import?аimpo trconstiimpo0_
  }
  
   {  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { useState, n(       
con ?
const content = `import?аimpo trconsti            
  }
  
   {  }
  
  const placeconst fs = require('fs');

con ?
const content = `import?аimport { usesca  :  .9  
  c     
con ?
const content = `import?аimpo5, cons: con ?
const content = `import?аimpo trconsti          cons    cssNam
con ?
const content = `import?аimpo2rec[4con ?
crdconsorconst content = `import?аimp     iv cl  
  ce= sp
con ?
const content = `import?аimpo   cons  con ?
const content = `import?аimpo trconsti        o-consr-  }
  
   {  }
  
  const placeconst fs = require(or   ' ro  
  c-5 0 
con ?
const content = `import?аimpo nacons'G  c     
con ?
const content = `import?аimpo5, c
 con ?
c  cons  const content = `import?аimpo trconsti    -5con ?
const content = `import?аimpo2rec[4con ?
crdconsorconstolcons 'crdconsorconst content = `import?аim       }
  
   {  }
  
  const placeconst fs = require('fso-  one-6  
  c     
con ?
const content = `import?аimpo (
cons  con ?
const content = `import?аimpo trconsti        seconsme  }
  
   {  }
  
  const placeconst fs = require(os  
      
  c     
con ?
const content = `import?аimpockeconsls  c     
con ?
con        className={cn(
          con ?
c  cons "const content = `import?аimpo trconsti     pcon ?
constd-[12px] hover:bg-white/[0.08] active:scale-[0.98] teconsefcrdconsorconst content = `import?аimp      ce= sp
con ?
const content = `import?аimpo   c  con ?
c
 cons  const content = `import?аimpo trconsti    la  
   {  }
  
  const placeconst fs = require(or   ' ro  un ed  
  csh do  c-5 0 
con ?
const content = `import?аirecon ?
cg-conse/con ?
const content = `import?аi           "bg-consie con ?
c  cons  const content = `  c  co  const content = `import?аimpo2rec[4con ?
crdconsorconstol9rcrdconsorconstolcons 'crdconsorconst conam  
   {  }
  
  const placeconst fs = require('fso-  one-6  
  c         
  c </di  c     
con ?
const content = `import?аimpo
 con ?
c  consimcons  con ?
const content = `im  const cont    
   {  }
  
  const placeconst fs = require(ol mx-auto re at  
  cex fl      
  c     
con ?
const contente)  c    con ?
concons 
con ?
con        className={cn(
          con nicon ={          con ?
c  cons   c  cons "const 
constd-[12px] hover:bg-white/[0.08] active:scale-[0.98] teadcon ?
const content = `import?аimpo   c  con ?
c
 cons  const content = `import?аimpo trconsti    la  
   {  }
: cons ec
 cons  const content = `import?аimpo ame=   {  }
  
  constoup relative flex w-full bg-[#0a0a0a]/60 back ro  csh do  c-5 0 
con ?
const content = `import?а  con ?
const conx_cons_-cg-conse/con ?
const content = `i0_const content5,c  cons  const content = `  c  co  const content = `i hcrdconsorconstol9rcrdconsorconstolcons 'crdconsorconst conam  
   {  }
  
 x_   {  }
  
  const placeconst fs = require('fso-  one-6  
  c]"  
  c       c         
  c </di  c     
con ?
const conor  c </di  c0.con ?
const coin:consow con ?
c  consimcons  con ?
c0,c  co0_const content = `im,2   {  }
  
  const placeconst fs =255,255,0. )]  cex fl      
  c     
con ?
const contente)  c     c     
con {/con ?
cd conss concons 
con ?
con        ivcon ?
camcon bs          con nicon ={  t-c  cons   c  cons "const 
constd-[12.0constd-[1nsparent opacity-const content = `import?аimpo   c  con ?
c
 cons  const contentr-c
 cons  const content = `import?аi      {/   {  }
: cons ec
 cons  const content = `import?аimam: consol cons  c-1  
  constoup relative flex w-full bg-[#0a0a0a]/80 ] con ?
const content = `import?а  con ?
const conx_cons_-cg-conse-focus-consinconst conx_cons_-cg-conse/con ?
duconst content = `i0_const conte"   {  }
  
 x_   {  }
  
  const placeconst fs = require('fso-  one-6  
  c]"  
  c       c         
  c </di  c     
con ?
const conor  c </di  c0.con    
 x_l= { opacity: 0, sc  c]"  
  c       c         
  c </di  c      1  c   e:  c </di  c     
co econ ?
const con 0consalconst coin:consow con ?
c  csic  consimcons  con ?
c}}c0,c  co0_const consN  
  const placeconst fs =255,255,0. 4p ]"  c     
con ?
const contente)  c     c     
con   con ?
cusconsn=con {/con ?
cd conss concons  cd conss cckcon ?=> fileInputRconcurcamcon bs          constd-[12.0constd-[1nsparent opacity-const content = `impojuc
 cons  const contentr-c
 cons  const content = `import?аi      {/   {  }
: 5 tr cons  const content =on: cons ec
 cons  const content = `import?аimam::t cons  ce   constoup relative flex w-full bg-[#0a0a0a]/80 ] con ?
meconst content = `import?а  con ?
const conx_cons_-cg-  const conx_cons_-c          <inputduconst content = `i0_const conte"   {  }
  
 x_   {  }
  
  const p    
 x_   {  }
  
  const placeconst fs = 
     
  cons    c  c]"  
  c       c         
  c </di  c     ag  c   df  c </di  c     
co  con ?
const con  cons < x_l= { opacity: 0, sc  c]"  
    c       c         
  c </d    c </di  c      1 ilco econ ?
const con 0consalconst coin:con ?onst co??  csic  consimcons  con ?
c}}c0,c  co0  c}}c0file && !copySiteMode   const placeconst fs =25dicon ?
const contente)  c     c     
con   con  0cons ycon   con ?
cusconsn=con {/ce=cusconsn=c: cd conss concons  c}
 cons  const contentr-c
 cons  const content = `import?аi      {/   {  }
: 5 tr cons  const content =on: cons ec
 cons  const contentat cons  const content =  : 5 tr cons  const content =on: cons ec
 cons  c[7 cons  const content = `import?аimam:  meconst content = `import?а  con ?
const conx_cons_-cg-  const conx_cons_-c          <inputd border-whiteconst conx_cons_-cg-  const conx_cur  
 x_   {  }
  
  const p    
 x_   {  }
  
  const placeconst fs = 
 55,0.05)] group/file cursor de  
  cons      x_   {  }
 iv  
  conse="p-     
  cons    c  c]" 0"  co    c       c      le  c </di  c     ag .5co  con ?
const con  cons < x_l= { opacit>
const co      c       c         
  c </d    c </di  c  "t  c </d    c </di  c 85const con 0consalconst coin:con ?onsap c}}c0,c  co0  c}}c0file && !copySiteMode   const placeconst fs =25die.const contente)  c     c     
con   con  0cons ycon   con ?
cusconsn=con ocon   con  0cons ytFocusLoss}
cusconsn=con {/ce=cusconsn=ce) cons  const contentr-c
 cons  const content = `i   cons  const content ==": 5 tr cons  const content =on: cons ec
 cons  cte cons  const contentat cons  const conad cons  c[7 cons  const content = `import?аimam:  meconst content = `import?а  con .5const conx_cons_-cg-  const conx_cons_-c          <inputd border-whiteconst conx_con   x_   {  }
  
  const p    
 x_   {  }
  
  const placeconst fs = 
 55,0.05)] group/file cursor de  
  cons={  
  constP omptText(e.target.value)}
     55,0.05)] group/file ctI  cons      x_   {  }
 iv  
  co={ iv  
  conse="p-   al  co
   cons    c  c]olconst con  cons < x_l= { opacit>
const co      c       c         
  c </dt-const co      c       c        bo  c </d    c </di  c  "t  c </dtecon   con  0cons ycon   con ?
cusconsn=con ocon   con  0cons ytFocusLoss}
cusconsn=con {/ce=cusconsn=ce) cons  const contentr-c
 cons  const content = `i   cons  const content ==": 5 trhicusconsn=con ocon   con  0coe]cusconsn=con {/ce=cusconsn=ce) cons  constyS cons  const content = `i   cons  const content =="76p cons  cte cons  const contentat cons  const conad cons  c[7 cons  const content = `impor"p  
  const p    
 x_   {  }
  
  const placeconst fs = 
 55,0.05)] group/file cursor de  
  cons={  
  constP omptText(e.target.value)}
     55,0.05)] group/file ctI  cons      x_   {  }
 iv  
  co={ iv  
  conse="p-   al  co
   cote ode && (
         
  consio .d 55,0.05)] group/file cal  cons={  
  constP omptText(e.tan  constP op     55,0.05)] group/file ctI  co o iv  
  co={ iv  
  conse="p-   al  co
   cons   :   co}}  conse="p     cons    c  c]olcutconst co      c       c         
  c </dt-const c    c </dt-const co      c       necusconsn=con ocon   con  0cons ytFocusLoss}
cusconsn=con {/ce=cusconsn=ce) cons  const contentr-c
 cons Scusconsn=con {/ce=cusconsn=ce) cons  constss cons  const content = `i   cons  const content ==":>
  const p    
 x_   {  }
  
  const placeconst fs = 
 55,0.05)] group/file cursor de  
  cons={  
  constP omptText(e.target.value)}
     55,0.05)] group/file ctI  cons      x_   {  }
 iv  
  co={ iv  
  conse="p-   al  co
   cote ode && (
         
  consio .d 55,0.05)] group/f   x_   {  }
 pa  
  conssc le 55,0.05)] group/file c    cons={  
  constP omptText(e.tFo  constP        55,0.05)] group/file ctI  cCop iv  
  co={ iv  
  conse="p-   al  co
   cote od8p  co-[  conse="pde   cote ode && (
  08         
  con-w  consio12  constP omptText(e.tan  constP op     55,0.0te  co={ iv  
  conse="p-   al  co
   cons   :   co}}  conse="p     cons   ha  conse="p_1   cons   :   co}} )]  c </dt-const c    c </dt-const co      c       necusconsn=con ocon   con  0cons okcusconsn=con {/ce=cusconsn=ce) cons  const contentr-c
 cons Scusconsn=con {/ce=cusconsn=ce) c 
 cons           key="send-button"
                init  const p    
 x_   {  }
  
  const placeconst fs = 
 55,0.05)] group/file cursor de  
  cons={  
  cont= x_   {  }
 0, scale: 0.5 }} 55,0.05)] group/file cab  cons={  
  constP omptText(e.tsN  constP 8p     55,0.05)] group/file ctI  co t iv  
  co={ iv  
  conse=" justify-center shadow-[  cox_  conse="p0,   cote ode && (
  px  _rgba(255,255,2  ,1)] hover:bg-[#eaeaea] hover:shadow-[0_4px_20px_r  co25  constP omptText(e.tFo  constP        55,0.0on  co={ iv  
  conse="p-   al  co
   cote od8p  co-[  conse="pde   cote       conse="pwU   cote od8p  co-[ re  08         
  con-w  consio12  constP omptTan  con-w  congr  conse="p-   al  co
   cons   :   co}}  consrm duration-300" strokeWidth={2   cons   :   co}}    cons Scusconsn=con {/ce=cusconsn=ce) c 
 cons           key="send-button"
                init  const p    
 x_   {  }
  
  const placeconst fs = 
 55,0.05)] group/file cursor de  
  cons={  
  cont= x_   {  }
 & cons           key="send-button"
     in                init  const p      x_   {  }
  
  const placeconsty:  
  cons      55,0.05)] group/file c y  cons={  
  cont= x_   {  }
 0,{{  cont= x:  0, scale: 0.5 }    constP omptText(e.tsN  constP 8p     55,0.05)] grid  co={ iv  
  conse=" justify-center shadow-[  cox_  conse="p0,   cote od    conse=" iv  px  _rgba(255,255,2  ,1)] hover:bg-[#eaeaea] hover:shadow
fs.write  conse="p-   al  co
   cote od8p  co-[  conse="pde   cote       conse="pwU   cote od8p  co-[ re  08         
